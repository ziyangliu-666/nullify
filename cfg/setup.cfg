// ============================================================
// setup.cfg — main entry point
// exec'd once via: +exec <name>/setup
// ticker loop driven by: -testscript ".../<name>/.vtest"
// ============================================================

sv_cheats 1

exec <name>/core/ticker.cfg
exec <name>/core/movement.cfg
exec <name>/jiting/engine.cfg
exec <name>/lunpan/setup.cfg
exec <name>/user/settings.cfg
exec <name>/user/keys.cfg
